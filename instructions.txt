Initialize npm and install dependencies  (npm init)
-Express, dotenv, cors, mongodb, ejs, nodemon (--save-dev)
-npm install express (node_modules + package-lock.json)
-dotenv - hide files/variables (npm install dotenv)
-cors - bypass cors errors (cross origin request). access things locally (npm install cors)
- ejs - html templating (npm install ejs)
-nodemon - auto restart connection (npm install nodemon --save-dev)

Require dependencies - setup in server.js file to enable dependencies

git init -> git repo ->
.gitignore - node_modules

Declare variables
Connect to mongodb - add connection string to .env file
Create port
Test mongo and port connection
Set middleware
Create gitignore file
Create Public and Views folders - add main.js and style.css to Public and index.ejs to Views
From root, init local git repo and commit
Create heroku repo
-Heroku login
-Heroku create simple-rap-api
-Echo “web: node server.js” > Procfile
-Git add.
-Git commit -m “changes”
-Git push heroku 