//1 Initialize npm and install dependencies  (npm init)
    -Express, dotenv, cors, mongodb, ejs, nodemon (--save-dev)
    -npm install express (node_modules + package-lock.json)
    -dotenv - hide files/variables (npm install dotenv)
    -cors - bypass cors errors (cross origin request). access things locally (npm install cors)
    - ejs - html templating (npm install ejs)
    -nodemon - auto restart connection (npm install nodemon --save-dev)

//2 Require dependencies - setup in server.js file to enable dependencies

git init -> git repo ->
.gitignore - node_modules

//3 Declare variables

//4 Connect to mongodb - add connection string to .env file - used to hide info and pw
    -mongodb - connect - connection your database - past string into .env and replace password
    - add .env to .gitignore

//5 Create port

//6 Test mongo and port connection - manually test (node server.js) or add nodemon 
    - add "dev": "nodemon server.js" to package.json file

//7 Set middleware - when data is flowing back and forth, middleware transform, read, format the data 
    - EJS templating allow us to run JS in html (shorthand tool)

//8 Create gitignore file
//9 Create Public and Views folders - add main.js and style.css to Public and index.ejs to Views
//10 From root, init local git repo and commit
//11 Create heroku repo
    -Heroku login
    -Heroku create simple-rap-api
    -Echo “web: node server.js” > Procfile
    -Git add.
    -Git commit -m “changes”
    -Git push heroku 